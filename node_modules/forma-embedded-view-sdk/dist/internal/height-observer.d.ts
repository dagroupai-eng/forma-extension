export declare class HeightObserver {
    #private;
    constructor(config: {
        onHeight: (height: number) => void;
    });
    observe(element: HTMLElement): void;
    unobserve(element: HTMLElement): void;
}
